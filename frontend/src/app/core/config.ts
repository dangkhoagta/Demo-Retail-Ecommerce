/**
 * Base URL of the ASP.NET Core API. The backend dev profile is pinned to
 * http://localhost:5080 (see backend Properties/launchSettings.json). Adjust here
 * if you run the API on a different port.
 */
export const API_BASE_URL = 'http://localhost:5080/api';

export const TOKEN_STORAGE_KEY = 'retail.auth.token';
export const CART_STORAGE_KEY = 'retail.cart';
